# Copy this file to .env and edit values for your domain.

# Endpoints
LLM_BASE_URL=http://127.0.0.1:8080/v1
EMBED_BASE_URL=http://127.0.0.1:8081/v1

# OpenAI-compatible model names used in requests
LLM_MODEL=gpt-3.5-turbo
EMBED_MODEL=text-embedding-3-small

# Storage
CHROMA_DIR=./chroma_db

# Retrieval defaults
TOP_K_DEFAULT=4
CHUNK_SIZE=320
CHUNK_OVERLAP=64
MIN_SOURCE_SCORE=0.20
STRICT_CONTEXT_MODE=true

# Windows log tool runtime
WINDOWS_LOG_QUERY_TIMEOUT_SEC=30
WINDOWS_LOG_MAX_EVENTS=200
WINDOWS_LOG_DEFAULT_HOURS_BACK=2

# Domain identity (visible inside system prompt)
DOMAIN_NAME=Windows OS logs and production incident analysis

# Domain system prompt (Mongolian)
SYSTEM_PROMPT_MN=Та бол Windows OS лог, production инцидент, алдааны root cause шинжилгээнд мэргэшсэн ахлах инженер. Та баримтгүй таамаг дэвшүүлэхгүй, evidence -> inference -> action дарааллаар бодож, хамгийн эрсдэл багатай засварын алхмыг зөвлөнө.

# Domain system prompt (English)
SYSTEM_PROMPT_EN=You are a senior engineer specialized in Windows OS logs and production incident root-cause analysis. Do not hallucinate; reason in evidence -> inference -> action order and provide the safest practical remediation steps.
